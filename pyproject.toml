[project]
name = "pluse-board"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
ingest = [
    "google-auth>=2.54.0",
    "google-auth-oauthlib>=1",
    "google-cloud-bigquery>=3",
    "requests>=2.34.2",
    "pydantic>=2",
]
dbt = [
    "dbt-bigquery",
]
