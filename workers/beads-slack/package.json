{
  "name": "beads-slack",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: Slack /task slash command -> structured via GPT -> GitHub repository_dispatch -> beads issue",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260628.1",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
